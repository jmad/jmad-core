language: java
sudo: false
jdk:
  - openjdk8
  - openjdk9
  - openjdk10
before_install:
  - chmod +x ./gradlew
script:
  - ./gradlew check
  - ./gradlew jacocoTestReport
after_success:
  - bash <(curl -s https://codecov.io/bash)
